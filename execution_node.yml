---
- name: Get execution node
  hosts: localhost
  gather_facts: yes

  tasks:
    - name: Capture execution node
      set_fact:
        exec_node: "{{ ansible_hostname }}"

    - debug:
        msg: "Execution node: {{ exec_node }}"
